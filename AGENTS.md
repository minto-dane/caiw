# caiw — Compressor for AI Weights

Lossless (byte-exact) archiver for safetensors model weights. Single dependency-free C file (pthread only).

## Build

```
gcc -O3 -march=native -Wall -Wextra -pthread -o caiw caiw.c -lm
```

## Usage

```
./caiw c out.caiw [-j N] [--ref base.st ...] in1.st [in2.st ...]
./caiw d out.caiw outdir/ [-j N] [--ref base.st ...]   # same --ref set+order as c
./caiw v out.caiw [-j N] [--ref base.st ...] in1.st [...]
```

- `c` compress, `d` decompress to safetensors, `v` verify against sources — BIDIRECTIONAL: every archive tensor must exist in the source with equal dtype+shape+bytes, every source tensor must appear in the archive (MISS-SRC), no duplicates (DUP), file set and `__metadata__` compared positionally (FILE/META).
- `-j N` threads (`-jN` also accepted; `-j` must be followed by digits). Default = online cores capped at 8, clamp 1..64. `-j 1` = fully sequential. Archives are thread-count independent on the decode side: an archive written at any -j decodes to identical bytes at any -j. Encoded bytes themselves may differ across -j (batch boundaries change which model state each tensor sees).
- `--ref` repeatable; best external reference is chosen per tensor by sampled escape rate and its file index is stored in the archive. Decode prefers the recorded index, then falls back to a name+len+dtype scan of all `--ref` files (`ref_resolve`) — a wrong-but-compatible ref decodes to wrong bytes and dies on the CRC check; a missing compatible tensor dies loudly. The archive records a file *index*, not ref identity — pair `--ref` files with external checksums if provenance matters.

## Format

`CAI5` magic (decoder/verifier also accept `CAI3`/`CAI4`). File table: `[u32 basename_len][basename][u32 meta_len][meta]` — `meta` is the source `__metadata__` object stored verbatim (absent in CAI3/4). Per-tensor record: name (raw bytes), dtype, shape (nd<=64), file idx, method byte, len, ref (intra idx for REF/DELTA, ref-file idx for DELTAX), payload len, CRC32, payload. Methods (low 6 bits): RAW PACK REF FIELD FIELDPOS FIELDROW DELTA U8 F32 DELTAX PRW. PRW carries no ref field; payload is `[u32 len][FIELD(row0)]` then per row `[u32 len][DELTA16 stream]` — each row's residual is coded against the previous row, and the per-row framing guarantees the referent row is fully decoded (incl. escapes) before use.

`safetensors` input parsing is real JSON, not substring matching: `jstr` decodes all escapes incl. `\uXXXX` (surrogate pairs -> UTF-8), `jspan` skips balanced objects/arrays string-aware, `jget` walks key:value pairs with proper value skipping. Tensor names are stored UNescaped in the archive and re-escaped (`jesc`) when `d` regenerates headers — escaped names like `a\"b` round-trip through the official loader. Enforced at load AND at archive-decode: `product(shape) * dtype_size == len` (rejects >64 dims, shape/len mismatch, negative dims); duplicate tensor names within a file are rejected; names/dtype strings must be strict UTF-8 (`utf8_ok` — no lone surrogates, overlongs, or >U+10FFFF) so regenerated headers are always valid JSON; `__metadata__` is validated by a real recursive-descent JSON grammar (`jval`/`jstrv`/`jnum`, depth-capped at 128) — not just brace balance — so malformed meta can never be replayed into output.

Method byte flags (CAI4+):
- `0x80` MF_BAT: tensor belongs to a parallel batch — decoded against the batch-start entropy snapshot.
- `0x40` MF_BH: first member (head) of a batch. Head = 0x80|0x40, members = 0x80, solo = no flags.
- Batches: encoder groups consecutive tensors whose every intra-archive ref (dup target + all DELTA candidates) predates the batch (`joinable()`); workers encode against a snapshot of committed hist state, then hist deltas merge (`worker += worker - snapshot`, counts are additive/order-free). Decoder rebuilds identical snapshots at MF_BH boundaries. Orphan member / ref into open batch = hard error.

## Test / regression

- `./test.sh` — self-contained regression: generates fixtures via `gen_fixtures.py` (pure Python, no numpy), exercises `-j1`/`-j4`, checks `0 bad`, PRW selection + round-trip on `rows.st`, metadata round-trip, incomplete-source `v` failure, and `tests/corpus/` crash inputs (captured from past mutation audits) must die cleanly.
- Synthetic fixtures in `out/` (generated): `edgeA/edgeB.st` (all dtypes, 0-dim, empty, dups, delta pairs), `ch*.st` (3-checkpoint f16 chain — DELTA must win), `f32A/B.st` (f32 pair), `exp.st` (10-tensor MoE-like), `pack1.st` (k=1 PACK — ib=0 edge), `escname.st` (quote/backslash/unicode names), `ndim9.st` (rank-9), `meta.st` (`__metadata__`), `split.st` (64 small bf16 tensors — teach/cold-start), `rows.st` (smooth-row F16 matrix — PRW must win + round-trip), `dx*.st` (DELTAX), `olmoA/B.st` (956MB real F32 pair, generated locally only).
- Models in `models/` (HF safetensors, local only). Verified: pythia pair (332t), MoE shard (1063t), qwen3-4b (398t), OLMo F32 pair (173t, DELTAX 170).

## Notes for contributors

- All methods compete by *actual encoded size*; the winner is always chosen by real bytes. Sampled estimates may only PRUNE candidates before trials (pos_gain>0.06, prw_gain bits/elem<9, sampled DELTAX escape rate) — never decide the winner. Document any addition.
- Prefix pre-filter (in `compete`, tensors ≥512K elems): every candidate is first encoded on a prefix of `min(ne/8, PREF_EL)` elements (FIELDROW caps the row count, keeping row length); candidates projected >1/6 + 256KB behind the best prefix are dropped without a full encode. This is still pruning, not selection — every survivor (including all prefix failures) gets a full-size trial, so the winner remains chosen by real bytes. Dropped candidates that would have won at full size are the only source of output change (rare; prefix rates track full rates within a few %). Prefix trials use the same TJob wave machinery as full trials — parallel on the solo path (psp=min(ntry,nc), TRYBUD-bounded), sequential inside batch workers (ntry=1). Do NOT skip pos_gain/prw_gain when prefix engages: a FIELDPOS prefix still pays the full K-context normalization per block (K depends on P, not the prefix length), so the sampled gains remain the cheaper pre-prune for large-K tensors.
- DELTA16 residual coding is conditioned on `ref>>9` (128 ctxs); DELTA32 XOR planes conditioned on `ref>>23` (256 ctxs). Context tables normalize only contexts used in the block (used-mask), decode side reconstructs the same set.
- Parallel candidate trials: each candidate encodes once against a clone of the same model state; smallest real size wins; only winner's hist delta commits — output identical to sequential competition. Trial buffers fold into a rolling best (`nsp` concurrent scratch + 1 retained), so TRYBUD is a real concurrent bound.
- Cold start: RAW/PACK winners emit no channel stream, so `teach()` counts their elements into the natural channel (CBF/CFP/C32/C8) on BOTH sides — encode (post-commit) and decode (post-payload). Any future streamless method must join `teach()` or document why not.
- Model histograms are `uint64_t` (no periodic rescale — rescale would desync the batch delta-merge). In-block cum/sym frequency tables stay u32/u16 (bounded by TOT=2^15).
- mmap everywhere: keep offsets page-aligned; drop consumed pages with `drop_pages`.
- DELTA16 escape values are coded through the shared FIELD channel (measured better than a dedicated channel).
- PRW (prev-row delta): F16/BF16 tensors with nd>=2, last-dim >=64, rows<=65536 may add a candidate that codes row i as kmap16 residual vs row i-1. Rows are SEPARATE DELTA16 streams (`dlt_enc_ws`/`dlt_dec_ws` share a `DltWs` scratch so per-row calls don't mmap-churn). A single whole-tensor stream is NOT safe: `dlt_dec`'s per-block used-mask reads the ref row up-front, which with a self-referential lag < BLK would read not-yet-decoded positions, and deferred escapes would still be 0 placeholders when read as refs. `prw_gain` prunes hopeless tensors by sampled residual bit-estimate before the real encode.
- Cold start: RAW/PACK winners carry no rANS streams, so without help a run of small tensors would keep the float channels at their all-ones init forever. `teach()` counts every RAW/PACK winner's elements into its natural channel (CBF/CFP/C32/C8) on BOTH sides — the decode replay is identical, so the model warms up even when RAW keeps winning. (Split-tensor fixt: 64x256x256 bf16 went from ~100% RAW to ratio 0.660, beating the single-tensor 0.666.)
- Candidate trials keep only the rolling best (buffer + hist clones), so peak try memory is ~(nsp+1) x ebound — `TRYBUD` now actually bounds concurrent scratch. Note ebound is VIRTUAL reservation; resident use tracks real encoded size.
- Histogram cells are `uint64_t` — no rescale needed; `hmerge` deltas stay exact for any archive size. `cum` prefix tables stay u32 (sums <= TOT=2^15).
- Encoder hist updates are FUSED into the backward rANS encode loop in every stream encoder (f16/f32/u8/pos/dlt16/dlt32) — the old separate forward re-read pass is gone; increments commute and the encoder only ever reads the normalized ft/cum snapshots, never h mid-block. pos_enc/pos_gain compute po/ctx incrementally (one seed division per block/call, then add/subtract steps — K<=P so ctx moves <=1 per po step; identical values, no per-element division). `pos_gain2` evaluates BOTH position gates (column + rowwise) in one sample pass with u32 histograms and a shared marginal — same counts, same gate decisions, half the data reads. pos_enc norms only the block's USED ctx range(s): po values in a block are contiguous (rowwise monotone, column run or wraparound pair) and ctx=po*K/P is monotone, so norm_ctx/cum run on [po_lo*K/P .. po_hi*K/P] only — per-ctx norms are independent so the emitted bitstream is unchanged and the decoder (full-K norm) needs no format hint. po may reach P when P∤n (ctx=K aliases the mantissa table area — self-consistent on both sides); the range clamps to K-1 since the mantissa norms overwrite those cells anyway.
- emit_rec's record CRC is precomputed inside ejob_run for batch members (overlaps sibling encodes; emit uses t->crc for bat>0, computes inline for solo).
- Two more parallelism levels, both byte-identical: (a) batch workers get a candidate-trial budget `ntry = g_threads/nm` (+remainder) so a batch smaller than the thread count still uses all cores for trials — candidate results are deterministic so the archive is unchanged; (b) a trial wave running `sp` jobs splits the budget as `nthr = ntry/sp` and f16_enc honors it with block-parallel encoding — each emitted block is a self-contained bitstream (x=LOWER + len in the header), so once a block's ft snapshot is fixed its encode is independent. Waves run [norm ft + count block into h] serially (the model dependency chain), then [backward rANS encode] in parallel into per-block buffers, then [emit_blk] serially in order. Only FIELD takes nthr>1 today (escapes/PRW side streams stay serial); other encoders keep their serial fused loops.
- CRC32: callers go through `crc32b`, which dispatches to a slicing-by-8 path (`crc32_slice`, tables derived mechanically from `crc_tab`, ~5x throughput on LE hosts) after a one-time differential self-test against the WP-proved scalar `crc32_of`; any mismatch (e.g. big-endian host, where the memcpy word load order differs) permanently falls back to scalar. `crc32_of`/`crc_setup` stay untouched so their WP proofs still stand; `crc32_slice`/`crc_setup8`/`crc32b` are unverified glue covered by the self-test + regression.
- Batching costs ~0.8% ratio on pythia pair (cold-start model state per batch) — inherent trade for parallelism. Tensors with promising in-batch refs are kept solo (`joinable`) so no DELTA/REF win is ever dropped.
- CAI5 vs CAI4: new archives preserve `__metadata__`; CAI3/CAI4 archives still decode but old `v` reports META when the source has metadata — that archive genuinely lost it.
- Host has ~8GB RAM — avoid loading multi-GB tensors into anonymous memory; numpy probes must use memmap + bounded samples.

## Hardening / verification status (2026-09)

Malformed-input contract: every archive/loader failure exits(1) with a `die()` message — never crashes, never OOB, never silent corruption of decoded bytes (CRC32 verified on every tensor; `c`/`d` outputs are all-or-nothing via tmp+rename with `atexit` cleanup). Scope notes: CRC32 is corruption *detection*, not authentication — an adversary can forge an archive that passes it (hash/sign the archive externally if provenance matters). "All-or-nothing" covers die()-driven failures; SIGKILL/power loss between renames can leave a partially-renamed set, and outputs are not fsync'd. mmap of an input truncated concurrently by another process can still SIGBUS — an OS-level race no userspace tool can fully close. Portability edges: member-name uniqueness is byte-exact — case-insensitive or Unicode-normalizing filesystems (APFS/NTFS) can collide two distinct members on disk; `d` keeps all `nf` outputs open at once, so an archive with more files than `RLIMIT_NOFILE` dies loudly (raise ulimit); archive framing fields are all explicitly little-endian, but element payloads are interpreted in host order — archives are portable across little-endian hosts only (x86/ARM-LE); on a big-endian host a foreign archive would fail loudly at CRC, never corrupt silently.

Decoder trust boundary checks (all present — do not remove):
- Record walk: `BND` before every field; `nd<=64`, `file<nf`, `ref<i`, `method<=M_PRW`, `plen` within buffer; `prod(shape)*dbits/8==len` (spec floor-div; sub-byte dtypes OK) enforced on records (decode AND verify; unknown dtypes are opaque 8-bit); `MF_BAT` orphan-member rejection; REF/DELTA member may not ref into its own batch (`ref >= bstart` → die) — parallel decode would otherwise read a not-yet-decoded (NULL) referent.
- `dec_tensor`: method/dtype coherence is canonical — FIELD/FIELDPOS/FIELDROW/PRW require is_flt16, F32 requires is_f32, DELTA/DELTAX require flt16|f32, U8 rejects is_flt16 (encoder emits U8 for every other dtype incl. integers/F64), PACK needs bsz<=2, RAW needs plen==len, REF needs plen==0, and in the record walk DELTA's referent must share the record's dtype and `__metadata__` is a reserved tensor name (prevents a duplicate-key header on regenerated output). Records the encoder can never emit are rejected outright (mirrors the encoder gates; PRW also needs nd>=2 + cols∈[64,2^28] + rows∈[2,65536] — the cols cap keeps a row's stream under the u32 length field).
- Resident decode set is bounded: `g_dlive` charges each in-flight tensor `len + dec_aux(method)` (DELTA16: escape values ≈len + bitmap ≈len/16 + possible misaligned-ref copy ≈len + ~3.3MB decode workspace — the encoder-only scr/cB/sB fields are skipped by `dltws_init_dec`; DELTA32 planes ≈len, DELTAX aligned ref copy ≈len, PRW per-row escape workspace ≈len/2 (escbuf sized cols*2 upfront), pos-ctx tables ≈14MB, 4MB fixed-workspace floor) plus a one-time charge for the record table + dup-name set (`NT×(sizeof(Tensor)+512) + dcap×16` — ≈22x archive-size amplification that would otherwise slip past the bound), and dies once the total exceeds MemAvailable (parsed from `/proc/meminfo`; NOT `_SC_AVPHYS_PAGES` — that returns freeram, which excludes reclaimable cache and would spuriously kill legit decodes; on non-Linux hosts lacking /proc/meminfo the AVPHYS fallback IS that too-strict metric). A decompression-bomb archive dies deterministically instead of being SIGKILLed mid-write by the OOM killer. Non-referenced RAW tensors bypass `dec_tensor` entirely (zero-copy: payload IS the tensor bytes — crc + teach + write/compare direct from the archive buffer), which also keeps them out of `g_dlive`. Remaining uncounted-but-bounded: per-batch hist snapshot (~44MB), winner payload buffers held until batch emit (≤BCAP), encode-side TRYBUD scope (virtual reservations, not resident).
- `d` writes `name.caiwtmp` then renames after all files close cleanly; rename targets are tracked in `g_outp` too, so a late failure unlinks already-renamed files — output is all-or-nothing *on die()-driven failure* (SIGKILL/power loss mid-rename can still leave a partial set; no fsync). `c` likewise writes `<out>.caiwtmp` and renames at the end — a failed encode leaves no half-archive. Temp opens use O_NOFOLLOW|O_NONBLOCK (planted symlinks/FIFOs in a shared outdir can't redirect or hang the write). Temp-name collision is rejected up front: a file-table entry equal to another member's `<name>.caiwtmp` would make the rename clobber the wrong output (crafted archives could pair `a` with `a.caiwtmp`). Path-identity checks (text + stat dev/ino + realpath-dir) run BEFORE the tmp open and cover inputs, --ref files, the archive itself, and both names (final + .caiwtmp) — they are best-effort, not a hard TOCTOU-proof boundary.
- `c` rejects input basenames that `d` cannot write (`\`, `.`, `..`, >3000B) — an unwritable archive is never produced. Self-destruction guards (`same_out_path`: byte-compare, then `stat` dev+ino, then realpath-canonicalized dir+basename): `c` refuses input/`--ref` paths equal to the output path — including symlink/hardlink/`dir/../x` aliases; `d` refuses a member whose output path is the archive itself (a symlinked outdir counts); `c` also refuses basename pairs that collide under `<name>.caiwtmp` (e.g. `a` + `a.caiwtmp`), so every emitted archive stays decodable. `-j`/​`--ref` memmoves keep argv's trailing NULL. `sizeof(size_t) >= 8` is a compile-time requirement — all length arithmetic is u64.
- `die()` is reentrant-guarded (a `dying` flag sends concurrent worker dies to `_exit`) — two threads racing into `exit()` can't re-run atexit handlers (exit is not thread-safe).
- `BND` is negative-subtraction safe (`q > bend` checked before the difference is taken). `data_offsets` accumulation in `d` is bounded by INT64_MAX (off_t is signed; a u64 wrap would make fseeko UB on crafted lens). The dup-name table caps at 2^30 entries — the earlier 2^32 bound could make `dcap` wrap to 0 and index an empty table.
- `c` preprocessing (dup/dref/fref resolution) uses newest-first hash chains keyed by (len,fnv), name, and normalized family name — identical "latest match" semantics to the old O(NT²) scan, near-linear in practice. Byte-identical output verified on the pythia pair.
- `prw_dec`: per-row `[u32 len]` framing, `l1`/`lr` bounds-checked against the payload limit, and `in == lim` exact-consumption enforced at the end.
- Payload decoders take `lim` (payload end) and ALL enforce exact consumption — `rp == lim`/`in == lim` at exit, so trailing garbage inside a record is rejected rather than ignored: `read_blk` validates 12-byte block header + block len; `pack_dec` validates k∈[1,256], dict bytes, `idx<k`, exact bit-position bounds (`in + ((bit+ib-1)>>3) < lim` — a looser check permits a 1-byte over-read at payload end) AND `lim - in == ceil(ne*ib/8)` at exit. `dlt_dec_ws` checks the stored escape count against decoded escapes and requires `rp == lim` when the count is 0 (the nonzero case is exact via `f16_dec`'s own tail check).
- `st_load`: real JSON string/object handling (escapes, nested braces, surrogate pairs); header len ≤ 2^30; name ≤ 65535B, dtype ≤ 64B, nd ≤ 64; `b>=a`, `off+len` non-wrapping, `prod(shape)*dbits(dtype)==len`, dup names rejected via open-hash (not O(n²)), `__metadata__` span is ck_meta-validated at load (object/array/string or strict JSON literal — crafted scalars like `12x` are rejected), and `dsz > filesize - 8 - hl` via fstat — subtraction form, because `8+hl+dsz` itself wraps on near-UINT64_MAX data_offsets (AFL-found real SIGBUS).
- File table (d AND v): `nf`/`nfa` ≤ 65535 (u16 idx), per-name len bound + embedded-NUL rejection + `..`/`.`/`/`/`\` rejection (d side), dup filenames rejected via open-hash; CAI5 `__metadata__` replay is ck_meta-gated — malformed meta can never reach regenerated JSON.
- Thread dispatch: every `pthread_create` site records success in `job.spawned`; joins are gated on it — inline fallback leaves `th[]` entries unjoined (joining an uncreated pthread_t is UB; glibc's ESRCH was luck). Worker `gh`/`gsnp` are save/restored around inline AND threaded runs so the main thread's channel pointers never dangle into a freed snapshot.
- `-j` accepts digits only (`-j 12x` dies); a non-digit `-j*` token falls through to input parsing and dies on open.
- `pos_enc`/`pos_dec`/`pos_gain`: `P<=0` rejected AND `D=n/P` clamped to >=1 — a degenerate shape with P>n gave D=0 → `gi/D` SIGFPE (AFL-found).
- Encode output buffer: `ebound(len)=4*len+len/1024+64MB` is a proven upper bound — ≤2 emitted bytes per enc() call; FIELD does 3 calls/elem (3*len); DELTA16 escapes recode through f16_enc (len+3*len=4*len — the max); DELTA32/F32 ≤2.5*len. Trial encodes write the full stream regardless of winner, so under-sizing overflows on adversarial inputs even when the method loses.
- Alignment: `alview()` supplies aligned copies for odd-offset safetensors data; DELTAX ref tensor is alview'd on the decode side too; sampling paths and `teach()` use memcpy loads (teach can see the unaligned archive payload on the zero-copy RAW path).
- `norm_ctx` uses a u128 multiply only when `tot >= 2^49` (u64 `h[i]*bud` can overflow past that; the wide path is a per-call branch so the hot loop stays u64).
- rANS state invariant: x ∈ [LOWER, 256·LOWER) at every enc/dec boundary (blocks init x=LOWER; enc preserves it — verified exhaustively). Encoder scratch is SCRSZ=BLK*16+64 (worst case 3 emitted bytes/enc call, ≤5 calls/elem).
- `tests/corpus/` keeps AFL-discovered crash inputs as a permanent regression corpus; `test.sh` runs the full suite (round-trips -j1/-j4 + DELTAX + crash corpus must-die-clean).

Verification performed:
- Sanitizers: ASan+UBSan clean (only ~KB exit-time leaks), MSan clean, TSan clean incl. Pythia pair (332t, all batch/DELTA/REF paths, byte-identical archive).
- Corpus provenance: past coverage-guided audits (AFL++) found 5 real crashes on the *st_load/compete* side — 3× SIGFPE (`pos_enc` D=0), 2× SIGBUS (fstat wrap) — all fixed.
- Formal verification (`verify/`, runner `verify/verify.sh`, env-overridable tool paths):
  - TLA+/TLC 2.19: `CaiwBatch` — batch merge protocol (batch-start snapshot, private worker clones, join barrier, additive order-free merge, dec/enc hist agreement) — all invariants hold. `CaiwOutput` — tmp→rename commit (no partial final, all-or-nothing cleanup). Deliberately flawed variants yield the expected counterexamples (lost update / partial final) — the models demonstrably detect the hazards they guard against. Design-level models: they verify the protocol abstraction, not the C code itself.
  - Alloy: `CaiwArchive` — record-structure invariants (strictly backward refs, canonical batch flags, no refs into open batch, orphan-member rejection, dup-free (file,name), method/dtype coherence, `__metadata__` reserved) all UNSAT at scope 8; weakened model flips exactly the two removed facts to SAT.
  - CBMC 6.6 (proofs on the real caiw.c via `#include`): kmap16 bijection; dsym cell selection (AW=32); norm_ctx output contract on both u64 and u128 paths (AW=2 — larger alphabets exceed solver limits); rANS single-symbol round trip for f ∈ {1,2,3,5,128,257,TOT} incl. emit-loop worst case; parser trust boundary — utf8_ok bounded scan (n≤16), hex4 exactly-4-byte reads, jstr bounded dst, jstr_skip/jspan NUL scans; pack_dec bit-index bounds (BL=32, n≤24 — tractable since the byte-level refactor, ~68s). All VERIFICATION SUCCESSFUL. Known BMC solver limits (documented bounds, NOT implementation proofs): general-domain rANS (nondet divisor — 64-bit division bit-blasting; tried minisat AND Z3/SMT2 under CBMC, and Bitwuzla/Z3/CVC5/Boolector under ESBMC — all time out; the algorithm itself is now proved in Coq, see below), jkey nested scans, utf8_ok n>16, norm_ctx AW>2, pack_dec BL>48 — ALL FIVE have ALGORITHM-level Coq proofs now (below); any residual C-level gap is covered by the exhaustive sweeps + the checked-in crash corpus. NOTE: glibc ≥2.41 headers need the `verify/cbmcinc/` shim (_FloatN decls unparsable by CBMC 6.6).
  - ESBMC 8.5 (optional local layer, `verify/esbmc_pack.c`): pack_dec bit-index bounds proved at BL=48, n≤36, bsz∈{1,2} — 27/27 properties in ~249s under Bitwuzla, past CBMC's reach (CBMC proves BL=32 in ~68s and is OOM-killed at BL=48; the pre-refactor u16-dict/memcpy code timed out at EVERY bound — the byte-level refactor is what made it solver-tractable). Non-vacuity is checked in-script (nonzero property count + unwinding assertions pass). BL=64/n≤48 exceeds resources under both engines. Binary is ~600MB — not in CI.
  - Coq/Rocq 9.2 (`coqc`+`coqchk` via opam switch `caiw-fc`; CI runs it in `verify-framac`): ALGORITHM-level interactive proofs of the exact domains every BMC backend timed out on. All zero-axiom, coqchk-clean. Prove ALGORITHMS (Z-arithmetic models); correspondence with the C code is by inspection — the models mirror the implementation line-by-line, and the C level is covered by bounded CBMC + exhaustive dynamic sweeps, same tier separation as TLA+/Alloy.
    - `verify/Rans.v` — rANS single-symbol round trip over the FULL general domain: `LOWER ≤ x < 256·LOWER`, `1 ≤ f ≤ TOT` (nondeterministic divisor), `0 ≤ c ≤ TOT−f`, incl. the emit/renormalize byte loop (≤2 bytes on this domain, case-split proved).
    - `verify/Utf8.v` — `uok s = true ⟺ wutf8 s` for ARBITRARY-length byte lists: accepts exactly RFC-3629 well-formed UTF-8 (rejects bad lead, bad/truncated continuation, overlongs, surrogates, >U+10FFFF). BMC topped out at n≤16 on the nested scan; the Coq proof is length-unbounded.
    - `verify/Norm.v` — `norm_ctx` output contract for ALL histograms and `aw ≤ TOT` (the real precondition: every call site passes aw ≤ 1024): `Σf = TOT` exactly, length preserved, all cells `0 ≤ f_i ≤ TOT`, support preserved (`h_i=0 → f_i=0`, `h_i>0 → f_i>0`), and the all-zero branch yields a uniform all-positive table. The residual is added at the first argmax — proved for ANY max-attaining index. Models exact-Z math; C's u64 path never overflows when tot < 2^49 and the u128 path is exact for h[i]*bud — correspondence holds for sum(h) < 2^64; past 2^64 the u64 `tot` accumulator itself wraps (found during model↔C review; real per-block histograms never approach it).
    - `verify/Pack.v` — `pack_enc`/`pack_dec` semantic round trip for ARBITRARY element counts and both atom widths: an honest encoder record (le4 k + le2 dict + ib-bit MSB-first indices) decodes back to the original data whenever `bsz | len` and `k ≤ 256`. BMC/ESBMC topped out at BL=48/n≤36 on the bit-index product state space; the Coq proof is length-unbounded. Scope: honest-encoder semantics only — hostile-input bounds/exact-consumption stay with the C-level layers.
    - `verify/Jkey.v` — `jkey` JSON key-scan over abstract byte lists: (a) `jkey_bound` — every returned offset is strictly inside the object span; (b) `jkey_fuel` — under the decoder contract (`jstr` consumes `1 ≤ k` bytes per key — progress only; the `e ≤ oend` bound lives inside `jkey_f`, so no decoder upper bound is needed) every outer iteration consumes ≥1 byte, so fuel beyond `length l` is provably irrelevant and the scan terminates within `length l + 1` iterations; (c) `jkey_sem`/`key_at` — a returned offset sits immediately after a `key`-matching quoted string + whitespace + colon + whitespace, i.e. it really is the value position of a key occurrence. The NUL-driven `jstr_skip`/`jspan` helpers are modeled on `skipn w l ++ rest` (object tail + buffer tail), incl. escape handling, negative-depth close poisoning (`--depth==0`), and NUL aborts. `jstr` itself is modeled CONCRETELY as `jstr_dec` (standard escapes, `\uXXXX`, UTF-16 surrogate-pair combination, NUL rejection, `i+1>=bs`/`i+4>=bs` capacity checks) — `jstr_dec_contract` discharges the progress contract and `jkey_c` chains (a)-(c) onto the real decoder. BMC unwinds >240s on the nested scan (even O=3/R=1/unwind 8 times out — harness kept as `PICK=6` in cbmc_parse.c, verify.sh records LIM); the Coq proof is length-unbounded.
  - Frama-C 33.0 EVA+RTE (`verify/framac_eva.c`, opam switch `caiw-fc`): 17 codec/parser kernel functions, 99% statement coverage, 74 alarms all of "unknown" class — triaged as analyzer-precision limits on NUL-driven scans and guarded bit reads; 0 invalid. Screening evidence, not a proof of absence.
  - Frama-C WP 33.0 (`verify/wp.c` + ACSL contracts inside caiw.c on p16le/p32le/p64le/g32le/g64le/fnv/crc_setup/crc32_of/hex4/utf8_ok/dsym/kmap16/kmap16_inv/kmap32/is_bf/is_f16/is_f32/is_flt16/mbits_of/dbits/ck_shape_len/enc/dec/emit_blk/read_blk/norm_ctx/dtb/joinable/ebound/dec_aux/r64/r32/r16/r8/u8_blk/u8_dec/dsym_raw/f16_elem/f16_blk/fill_ctx/norm_fM/f16_tab/f16_dec/pos_elem/pos_blk/pos_tab/pos_dec_ws/f32_elem/f32_blk/f32_tab/f32_dec_ws/pack_dec/dlt_val/dlt_elem/dlt_blk/dlt_tab/dlt_dec_ws/dlt_scatter/dlt_tail/d32_elem/d32_blk/d32_tab/d32_plane/dlt32_dec_ws/prw_dec_ws): deductive, UNBOUNDED per-contract proofs — 3022/3022 goals, zero timeout/unknown/failure. Proven: endian codecs touch only their contracted ranges; fnv reads stay in bounds, assigns nothing; crc32_of's writes are confined to crc_tab[0..255]+crc_ready (assigns completeness) and input reads stay in range; the dtype predicates (is_*/mbits_of/dbits) prove assigns \nothing + result ranges via Frama-C's bundled strcmp spec; ck_shape_len's shape-product loop (u128) reads only shape[0..nd-1] under nd<=64, die() paths spec'd; hex4/utf8_ok memory-safe + terminating + ∈{0,1} (utf8_ok under n≤2³⁰); dsym termination/bounds/*fc==f[r] plus proven exit asserts cum[r]≤v<cum[r+1] and f[r]>0 (dec() can't div-by-zero under a consistent normalized table, and *fc>0 exported as an ensures); kmap16/inv literal specs; **enc** preserves the rANS state band (f·2¹⁶≤x<256·LOWER in → LOWER≤result<256·LOWER out) and emits at most 2 bytes within *pp-2..*pp; **dec**'s read cursor advances monotonically and never passes `end`; **emit_blk** writes exactly [u32 len][8B state][payload] inside the contracted destination and returns o+12+len; **read_blk** validates the 12-byte header, checks bl against remaining bytes, and guarantees *end≤lim — die() paths carry an honest non-returning spec (terminates \false, exits 1); **u8_blk** (extracted per-block decode loop — stream cursor passed by value so no *rp assigns boundary) proves normalized-table safety: dsym domain v<cum[256], dec()'s f>=1 via the *fc>0 ensures, h-increment bound, stream-validity preserved through dec calls; **u8_dec** then closes the whole block loop — memory safety, per-cell h growth <= n, and exact-consumption \result==lim on the untrusted-input path; **r64/r32/r16/r8** (little-endian record readers) prove pointer advance + in-range reads. The **F16/FIELD decoder layer** is closed by deeper decomposition: **f16_elem** (one element: S/E/M symbol decodes via **dsym_raw** — a memory-safety-only binary-search variant whose contract needs no table semantics — with every dec() call guarded by a runtime `fc` check so div-safety is a path fact, not a quantified hypothesis), **f16_blk** (per-block loop, stream cursor by value), **fill_ctx** (per-context prefix-sum build — u32-safe by the linear `cc[i]<=65535*i` bound since f entries are u16, no table invariant needed), **norm_fM** (the 2ew-context norm_ctx sweep with an int64 index check per context), **f16_tab** (whole ft/cum construction), and **f16_dec** (block loop + exact consumption `rp==lim` + h-growth bound). ew/mw enter as literals per mb∈{7,10} branch — semantically identical, keeps `1<<(15-mb)` shift evaluation out of every goal. The **FIELDPOS/FIELDROW decoder** is closed the same way, with heap-workspace threading: pos_dec is now a thin alloc+call+free shell around **pos_dec_ws** (workspace passed as pointers — xm/xc stay outside WP since \fresh cannot re-export malloc's spec), which proves the block loop + exact consumption + h-growth bound over the 2,162,688-cell pos histogram; **pos_tab** builds the K-ctx SE tables + M tables via accumulator-indexed loops (`off`/`fo`/`k2` walk linearly — no product-index goals at all); **pos_blk** computes `po = gi%D or gi%P`, dies on `po>=P` (unreachable for real shapes where P|n; crafted archives die loudly), and derives `ctx` via `(__int128)po*K/P` (128-bit product keeps overflow out of the goal entirely); **pos_elem** decodes SE/M via dsym_raw with int64 ctx-index checks supplying every table bound as a path fact. The **F32 decoder** reuses the identical template at 5 decode stages (S/E/m1/m2/m3, se<=511 ctx, fixed 328194-cell ft / 329730-cell cum workspaces): f32_elem (5 dsym_raw + 5 runtime fc guards + int64 product checks), f32_blk (block loop), f32_tab (5 norm sweeps + 4 fill_ctx sweeps over accumulator indices ko/fo with `ko == c*(aw+1)`/`fo == c*aw` equality invariants — the equalities, not the bounds, are what let call-site requires instantiate), f32_dec_ws (block loop + exact rp==lim). The **PACK decoder** proves bit-extraction memory safety + exact consumption (`lim - in == ceil(ne*ib/8)` at exit): `k∈[1,256]`/`idx<k` stay runtime-checked (the checks themselves supply every bound downstream), bit reads go through `/8`/`%8` arithmetic forms, and the `ib` width loop uses a doubling `p2` accumulator with an unreachable `ib>=8` die-guard (k≤256 makes it dead code — the guard, not lsl reasoning, supplies the bound). The **DELTA16 layer** is closed by full decomposition: **dlt_val** (kmap16/kmap16_inv value mapping extracted non-inline — its bit_test chain used to poison every enclosing goal), **dlt_elem** (one symbol: ctx = ref>>9 rendered as `/512`, u16-load upper bound supplied by an explicit assert — the proof model does not infer C integer-type ranges — and a `(uint32_t)` cast ahead of the shift so every stage stays nonneg), **dlt_blk**, **dlt_tab** (per-ctx norm_ctx + fill_ctx over accumulator offsets; the earlier `fo<=520319` invariant was a real bound bug — correct max 524416 for c=128), **dlt_dec_ws** (workspace form; the per-element escape list that used to realloc mid-loop is now an `escbits` bitmap — escape count derived as popcount, no pointer-count invariant needed), **dlt_scatter** (bitmap-driven escape collection), **dlt_tail** (escapes decoded through f16_dec with its own exact-consumption check), and zero-escape payloads close with `rp==lim`. The **DELTA32 layer** repeats the template over four byte planes: **d32_elem** uses a saturating histogram increment (`if (hp[i] != UINT64_MAX) hp[i]++` — unreachable for counts, removes every u64-overflow precondition at once), **d32_blk**/**d32_tab** mirror DELTA16, **d32_plane** (the whole per-plane pass extracted as a function boundary — its ensures carry the per-cell `+n` growth bound, so the outer level never needs a cross-plane invariant — a mixed-plane `h[j]<=Pre+c0` invariant is FALSE from pass 2 on: earlier planes' cells already grew), and **dlt32_dec_ws** calls the four planes unrolled with literal offsets `h+0/65536/131072/196608` — literal indices make every slice instantiation, separation fact and assigns-disjointness trivially decidable; the variable product `p*65536` never appears in a goal (the same goal with a symbolic `p*65536` index drowns e-matching — verified by minimal repro). The **PRW decoder** (`prw_dec_ws`, thin alloc shell around it) proves the row framing: `pos` linear cursor instead of `r*cols` (multiplication drowns the context), per-row `[u32 len]` bounds `l1`/`lr`, exact `in == lim`, and the workspace-slice separation facts stated as asserts. Limits: dsym's containment as an ensures hits Z3 quantifier Unknown (kept as proven exit assert; its *fc>0 consequence IS exported and proved); kmap16 round-trip composition needs bitwise↔arith bridging lemmas Qed lacks — CBMC covers all 65,536 inputs, so no evidence gap; **norm_ctx** is now contracted — memory safety of both branches, dominant-cell bounds, zero-support preservation (h[i]==0 → f[i]==0), **positive support (h[i]>0 → f[i]≥1 — what decoders need for div-safety)**, uniform f[i]≤TOT bound, all-zero-branch positivity — under requires 1≤aw≤32768 (bud≥0; real callers pass aw≤1024) and h[i]≤2^48-1 (tot=Σh exact in u64; callers outside WP scope → assumption). The final dominant-cell add is gated by `(rem>0)` (identical on all reachable states — rem≥0 always: Norm.v proves it, sweeps check it) so the ensures hold without discharging the Σq≤TOT quotient-sum bound in WP; the same gate is toxic to CBMC (it makes sum==TOT non-structural — the solver must prove rem≥0, i.e. the quotient-sum theorem itself, through the division circuit), so `#ifdef __CPROVER__` shows CBMC the unconditional add — the two forms differ only on the unreachable rem<0 — that bound needs partial-sum reasoning whose quantifier instantiation drowns Z3 (ghost prefix arrays AND axiomatic recursive sum both attempted, reverted, documented). Σf==TOT/rem≥0 stay with Rocq+sweeps; pointer-scan parsers (jstr/jkey/jget) sit behind Qed's missing strlen/valid_read_string model; the u8 DECODE layer is now closed by decomposition (u8_blk above); the u8 ENC side resists the analogous extraction — its backward-moving write cursor needs a range-assigns frame whose Qed encoding drowns the same preservation goals (encoder input is trusted: CBMC + sweeps + Norm.v own it); xm's \fresh can't re-export malloc's bundled spec; the dec/read_blk \base_addr ensures, dsym exit asserts, and both U8 paths' cum[256]==TOT runtime guards (anchoring Norm.v's proof, same pattern as enc's f==0 die) remain. Every assigns clause that writes or returns a pointer carries a \from functional-dependency set (the pedantic warnings are all discharged): cursor stores depend on the old cursor + the inputs deciding advancement, read_blk/emit_blk \result on the header bytes/offsets, u8_dec \result \from lim literalizes exact consumption. Rule: every in-tree annotation is machine-checked — nothing unproved is left in caiw.c. ACSL lives in `/*@ */` comments — invisible to gcc; `make verify` exercises it.
  - Exhaustive C sweeps (dynamic, not proofs — checked-in reproducer `verify/sweeps.c`, run by verify.sh): 589,809-case rANS boundary sweep (every f × every emit-count boundary — byte counts change only at XMAX·256^k; asserts round trip + rp==end exact consumption); 3,264 histograms × all 32,768 v for dsym (106,954,752 selections).
- CI (`.github/workflows/ci.yml`): `test` job = -O3/-march=native + -Werror + portable-baseline builds and `test.sh` under ASan+UBSan (detect_leaks=0 — exit-path allocations are deliberate). `test-arm` job = same build + `test.sh` on ubuntu-24.04-arm (real aarch64 hardware — exercises the little-endian portability claim). `test-tsan` job = `test.sh` under ThreadSanitizer (data races in the -j4 parallel paths fail the run). `verify` job downloads tla2tools/alloy jars into `verify/tools/` (gitignored) and runs `verify.sh` — TLA+/Alloy/CBMC all pass in CI. `verify-framac` job builds a cached opam switch (frama-c 33.0 / why3 1.8.2 / alt-ergo-free 2.4.3 + coq 9.2.0 / rocq-stdlib 9.1.0, all pinned to the local proven set) + z3 4.13.3 release binary into `verify/tools/bin/` — EVA/WP layers AND the five Coq proofs all run in CI; first uncached run ~60min (Rocq builds from source), cached runs ~3min; a weekly cron keeps the cache entry alive.
- Static analyzers: gcc -fanalyzer triaged (real bugs fixed), scan-build-19 (post-fix run: only exit-path leak notes + escbuf size-0 FP — alloc is ≥2KB always), clang-19 --analyze (remaining items are analyzer FP — untracked invariants), cppcheck 2.17.1 clean (user-space patched binary; std.cfg via /tmp/cfg symlink).
- Valgrind 3.26 (user-space build): static-linked caiw runs clean — all reports are the known static-glibc startup FP class (`__strcmp_avx2` reading vector-width chunks incl. uninit tail bytes of argv/execfn buffers, which valgrind's dynamic-only interceptors can't suppress). MSan cross-confirms zero uninit flow in caiw code.
